@import("../../../../stylus/index.styl");
.list 
    overflow hidden
    .list-item
        width 100vw
        overflow hidden
        margin: $margin 
        padding: $padding
        display: block
     .imgbox
        float left
        border-radius 0.15rem
        overflow hidden
        width $listImgwidth
        height $listImgwidth
        img 
          width 100%
          height 100%
      .titlebox
        float left
        padding-left $padding
        .goodsname
            font-size $h2
            margin-bottom $margin  
        .price
            font-size $h3
            color $primary
            margin-bottom $margin 
        .an
            width $listbtnwidth
     